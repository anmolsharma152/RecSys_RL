# RecSys RL — setup

Status: [STATUS.md](./STATUS.md). Theory walkthroughs under `docs/0*.md`.

```bash
cd ~/Projects/RecSys_RL
python -m venv .venv && source .venv/bin/activate
# pip install -e .  or use pyproject / uv per README
python train.py
```
